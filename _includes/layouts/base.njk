<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <meta name="description" content="{{ description or metadata.description }}">
    <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
    <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">
  </head>
  <body>
    <div class="wrapper page">
      <header class="header">
        <h1>
          <a href="{{ '/' | url }}" class="logo">
            <div class="logo__group-1">
              <span>Vallekas</span>
              <span>con</span>
            </div>
            <span class="logo__group-2">k</span>
            <div class="logo__group-3">
              <span>de</span>
              <span>Kultura</span>
            </div>
          </a>
        </h1>

        {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
        {# <ul class="nav">
        {%- for entry in collections.all | eleventyNavigation %}
          <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"><a href="{{ entry.url | url }}">{{ entry.title }}</a></li>
        {%- endfor %}
        </ul> #}
      </header>

      <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>
        <div class="max-ch tc text-wrapper">
          {{ content | safe }}
        </div>
      </main>

      <footer class="footer">
        <nav class="nav nav--horizontal">
          <ul>
            <li><a href="mailto:vallekaskultura@gmail.com">Escríbenos</a></li>
            <li><a href="https://twitter.com/conKdeKultura" target="_blank" rel="nofollow">Twitter</a></li>
            <li><a href="https://www.instagram.com/vallekasconkdekultura/" target="_blank" rel="nofollow">Instagram</a></li>
          </ul>
        </nav>
      </footer>
    </div>  
    <!-- Current page: {{ page.url | url }} -->
  </body>
</html>
